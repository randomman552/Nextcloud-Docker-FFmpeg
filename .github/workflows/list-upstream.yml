name: List upstream tags

on:
  workflow_dispatch:

jobs:
  get-tags:
    runs-on: ubuntu-latest
    steps:
      - name: Fetch tags from upstream image
        run: |
          curl -s "https://hub.docker.com/v2/repositories/_/nextcloud/tags/?page_size=100" \
            | jq -r '.results[].name'
